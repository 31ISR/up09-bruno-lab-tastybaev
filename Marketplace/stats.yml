info:
  name: stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/stats"
  body:
    type: json
    data: |-
      {
        "totalItems": 15,
        "totalUsers": 8,
        "totalBids": 42,
        "activeItems": 12,
        "totalValue": 750000,
        "averageItemPrice": 25000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
